// lib/data/invoices.ts — Start-Beispielrechnungen für das Rechnungen-Modul.
// Decken alle Mahnstufen, Kundentypen und Zahlungsstatus ab. Mahnstufe &
// Überfälligkeit werden live aus due_date berechnet (siehe lib/invoices.ts).

// Keine Beispieldaten: Das Cockpit startet leer und wird ausschließlich
// mit echten, nachvollziehbaren Daten befüllt.
export const invoices = [];
